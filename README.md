# AE-CNN-INTRUSION-DETECTION
An AE-CNN ensemble-based Intrusion Detection System for multi-class network traffic classification. Uses Autoencoders for dimensionality reduction and a CNN-ensemble voting mechanism to achieve 96-98% accuracy. Features include SMOTE for class imbalance and data preprocessing with Scikit-learn and TensorFlow.
